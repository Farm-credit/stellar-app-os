/**
 * GDPR export/erasure (recreated — module was lost in a bad merge on main).
 * Implementation lives in lib/gdpr.ts; this alias keeps the historical
 * `@/lib/compliance/gdpr` import path used by the compliance reports route.
 */

export { exportUserData, deleteUserData } from '@/lib/gdpr';
